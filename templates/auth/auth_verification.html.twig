{% extends 'base.html.twig' %}

{% block title %}Verificación de Cuenta{% endblock %}

{% block body %}
<div class="container mx-auto max-w-md mt-10 shadow-md bg-white p-6 rounded-lg">
    <h1 class="text-2xl font-bold text-center mb-4 text-primary">Verifica tu Cuenta</h1>
    
    {% if app.flashes('success') %}
        <div class="alert alert-success text-center mb-4">
            {{ app.flashes('success')|first }}
        </div>
    {% endif %}
    
    {% if app.flashes('error') %}
        <div class="alert alert-danger text-center mb-4">
            {{ app.flashes('error')|first }}
        </div>
    {% endif %}
    
    <p class="text-sm text-gray-600 text-center mb-6">
        Se ha enviado un enlace de verificación a tu correo electrónico. Por favor, revisa tu bandeja de entrada y sigue las instrucciones.
    </p>
    
    <form method="post" action="{{ path('auth_resend_verification') }}" class="text-center">
        <button type="submit" class="btn btn-primary">
            Reenviar Correo de Verificación
        </button>
    </form>
    
    <div class="text-center mt-3">
        <a href="{{ path('auth_login') }}" class="text-decoration-none">Volver al Inicio de Sesión</a>
    </div>
</div>
{% endblock %}
