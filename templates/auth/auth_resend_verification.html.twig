{% extends 'base.html.twig' %}

{% block title %}Reenviar Correo de Verificación{% endblock %}

{% block body %}
<div class="container mx-auto max-w-md mt-10 shadow-md bg-white p-6 rounded-lg">
    <h1 class="text-2xl font-bold text-center text-gray-700">Reenviar Correo de Verificación</h1>
    
    <p class="text-center text-gray-600 mt-4">
        Si no has recibido el correo de verificación, puedes solicitar que se reenvíe ingresando tu correo electrónico.
    </p>

    <form method="post" action="{{ path('auth_resend_verification') }}" class="mt-6 space-y-4">
        <div>
            <label for="email" class="block text-sm font-medium text-gray-700">Correo Electrónico</label>
            <input type="email" id="email" name="email" class="w-full px-4 py-2 border rounded-md" placeholder="Ingresa tu correo" required>
        </div>
        <div>
            <button type="submit" class="w-full bg-blue-500 text-white py-2 rounded-md hover:bg-blue-600">
                Reenviar Correo
            </button>
        </div>
    </form>

    <div class="text-center mt-4">
        <a href="{{ path('auth_login') }}" class="text-blue-500 hover:underline">
            Volver al Inicio de Sesión
        </a>
    </div>
</div>
{% endblock %}
