{% extends 'base.html.twig' %}

{% block title %}Sistema 911{% endblock %}

{% block body %}
<div class="min-h-screen flex items-center justify-center bg-gray-100">
    <div class="max-w-4xl w-full bg-white shadow-md rounded-lg p-8 flex flex-col md:flex-row">
        <div class="md:w-2/3 flex flex-col items-start justify-center">
            <h1 class="text-5xl font-bold text-purple-600 mb-4">Sistema 911</h1>
            <p class="text-gray-700 text-lg">
                Bienvenido al Sistema 911: Un portal diseñado para reportar y gestionar emergencias de manera rápida y segura, conectándote directamente con las autoridades. ¡Tu seguridad es nuestra prioridad!
            </p>
        </div>
        <div class="md:w-1/3 mt-8 md:mt-0">
            <form method="POST" action="{{ path('usuario_login') }}" class="bg-gray-50 p-6 rounded-lg shadow-md">
                <div class="mb-4">
                    <label for="email" class="block text-gray-700">Correo electrónico o número de teléfono</label>
                    <input type="text" id="email" name="email" class="w-full mt-1 p-2 border rounded-md" required>
                </div>
                <div class="mb-4">
                    <label for="password" class="block text-gray-700">Contraseña</label>
                    <input type="password" id="password" name="password" class="w-full mt-1 p-2 border rounded-md" required>
                </div>
                <button type="submit" class="w-full bg-purple-600 text-white py-2 rounded-md">Iniciar sesión</button>
                <div class="text-center mt-4">
                    <a href="{{ path('auth_forgot_password') }}" class="text-purple-600 hover:underline">¿Olvidaste tu contraseña?</a>
                </div>
                <div class="text-center mt-4">
                    <a href="{{ path('usuario_registro') }}" class="bg-green-500 text-white py-2 px-4 rounded-md hover:bg-green-600">Crear cuenta nueva</a>
                </div>
            </form>
        </div>
    </div>
</div>
{% endblock %}
