{% extends 'base.html.twig' %}

{% block title %}Sistema 911{% endblock %}

{% block body %}
<div class="d-flex align-items-center justify-content-center vh-100" style="background-color: #f8f9fa;">
    <div class="row w-75">
        <!-- Texto de bienvenida -->
        <div class="col-md-6 d-flex flex-column justify-content-center align-items-start text-start">
            <h1 class="display-4 fw-bold text-purple">Sistema 911</h1>
            <p class="lead text-secondary mt-3">
                Bienvenido al Sistema 911: Un portal diseñado para reportar y gestionar emergencias de manera rápida y segura, conectándote directamente con las autoridades. ¡Tu seguridad es nuestra prioridad!
            </p>
        </div>
        <!-- Formulario de inicio de sesión -->
        <div class="col-md-6 d-flex justify-content-center">
            <div class="card shadow-lg p-4" style="width: 100%; max-width: 400px;">
                <form method="post">
                    <div class="mb-3">
                        <label for="email" class="form-label">Correo electrónico o número de teléfono</label>
                        <input type="text" id="email" name="email" class="form-control" placeholder="Correo electrónico o teléfono" required>
                    </div>
                    <div class="mb-3">
                        <label for="password" class="form-label">Contraseña</label>
                        <input type="password" id="password" name="password" class="form-control" placeholder="Contraseña" required>
                    </div>
                    <div class="d-grid">
                        <button type="submit" class="btn btn-purple btn-lg">Iniciar sesión</button>
                    </div>
                    <div class="text-center mt-3">
                        <a href="{{ path('auth_forgot_password') }}" class="text-decoration-none text-purple">¿Olvidaste tu contraseña?</a>
                    </div>
                    <div class="text-center mt-3">
                        <a href="{{ path('usuario_registro') }}" class="btn btn-success btn-lg w-100">Crear cuenta nueva</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}
