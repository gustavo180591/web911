import { startStimulusApp } from '@symfony/stimulus-bundle';

const app = startStimulusApp();
// registro any custom, 3rd party controllers here
// app.registro('some_controller_name', SomeImportedController);
